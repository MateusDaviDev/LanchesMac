@model IEnumerable<LanchesMac.Models.Lanche>

<div>
    <h2>Titulo: @ViewData["Titulo"]</h2>
    <p>Total de lanches: @ViewBag.Total</p>
</div>

@foreach(var lanche in Model)
{
    <div>
        <h4>@lanche.Nome</h4>
        <p><img src="@lanche.ImagemUrl"></p>
        <p>Preço : @lanche.Preco.ToString("c")</p>
    </div>
}